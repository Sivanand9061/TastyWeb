
  # Fix UI Issues

  This is a code bundle for Fix UI Issues. The original project is available at https://www.figma.com/design/kbABr9ZhTiwZKrUtjYsBoa/Fix-UI-Issues.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  